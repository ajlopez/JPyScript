class Repository:    def __init__(self):        self.items = []        self.maxid = 0            def add(self, item):        self.maxid = self.maxid + 1        item.id = self.maxid        self.items[item.id] = item            def get_all(self):        result = []                for item in self.items:            result.push(item)                    return result